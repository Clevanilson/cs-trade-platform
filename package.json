{
  "name": "cs-trade-platform",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "lint": "docker run --rm backend bun biome lint .",
    "lint:fix": "docker run --rm backend bun biome lint . --write",
    "lint:ci": "biome lint .",
    "test": "docker run --rm backend bun test",
    "test:ci": "bun test",
    "test:watch": "bun test --watch",
    "backend:dev": "bun run --cwd apps/backend dev",
    "backend:build": "docker build -f ./apps/backend/Dockerfile -t backend .",
    "backend:start": "docker run --rm backend",
    "backend:test": "docker build -f ./apps/backend/Dockerfile -t backend . && docker run --rm backend bun test",
    "services:up": "docker compose up -d",
    "services:down": "docker compose down"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "5.7.3",
    "lefthook": "^1.11.0"
  }
}

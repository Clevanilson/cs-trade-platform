{
  "name": "@cs-trade-platform/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "migrations:create": "bun run src/infra/database/create-migration.ts"
  },
  "dependencies": {
    "@cs-trade-platform/shared": "workspace:0.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5.7.3"
  }
}
